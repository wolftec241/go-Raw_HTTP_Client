package main

import (
	"github.com/wolftec241/go-Raw_HTTP_Client/server"
)

func main() {
	//go server.StartServer()
	//time.Sleep(500 * time.Millisecond) // wait for server to be ready
	//client.StartClient()
	server.StartServer()
}
